  var assert = require('assert')

describe('data aggregator main', function() {
  describe('datasource template', function() {
    //given the correct config, can be initialized
    it('should be able to load from a folder', function() {
      assert.equal(true, false);
    });
  });

  describe('datasource', function() {
    //given the correct config, can be initialized
    it('should be able to load from a folder', function() {
      assert.equal(true, false);
    });
  });

  describe('vertex client', function() {
    //given the correct config, can be initialized
    it('should be able to load from folders', function() {
      assert.equal(true, false);
    });
  });

  describe('data aggregator', function() {
    //given the correct config, can be initialized
    it('should be able to load from folders', function() {
      assert.equal(true, false);
    });
    //finds the blockchain client (autogen) ? if not already there
    //generates the vertex client using the schema (autogen) ? if not already there
    it('should be able to create datasources during setup', function() {
      assert.equal(true, false);
    });
      //abi code import
    it('should be able to start', function() {
      assert.equal(true, false);
    });
    //creates function handlers for the smart contracts involved with name using abi file name
    //correctly adds //contract creation files successfully
    it('should be able to stop', function() {
      assert.equal(true, false);
    });
    //imports of blockchain client, vertex client) //contract creation files successfully
    it('should be able to initialize blockchain client correctly', function() {
      assert.equal(true, false);
    });
    //dockerfile builds   //dockerfile? (build)  //dockerfile (tests?) (or docker-compose tests)
    it('should be able to', function() {
      assert.equal(true, false);
    });
  });


});
